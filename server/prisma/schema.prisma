 // Это ваш файл схемы Prisma.

// Узнайте больше в документации: https://pris.ly/d/prisma-schema



generator client {

provider = "prisma-client-js"

}



datasource db {

provider = "sqlite" // Используем SQLite для простоты (файл dev.db)

url = "file:./dev.db"

}



// --- ПОЛЬЗОВАТЕЛЬ ---

model User {

id Int @id @default(autoincrement())

username String @unique

password String

avatar String? // Ссылка на аватарку

  status      String   @default("offline") // online, offline

  lastSeen    DateTime?

  

// Список друзей хранится как JSON массив ID "[1, 2, 5]"

// (простой способ, чтобы не делать сложную many-to-many связь сейчас)

friendsData String?



// Связи

servers Member[] // В каких серверах состоит

owned Server[] // Какие сервера создал

messages Message[] // Отправленные сообщения

reactions Reaction[] // Поставленные реакции

  

// Уведомления

sentNotif Notification[] @relation("Sender")

recvNotif Notification[] @relation("Receiver")

}



// --- СЕРВЕР ---

model Server {

id Int @id @default(autoincrement())

name String

icon String?

description String?

  

ownerId Int

owner User @relation(fields: [ownerId], references: [id])



channels Channel[]

members Member[]

invites Notification[] // Приглашения на этот сервер

}



// --- УЧАСТНИК СЕРВЕРА ---

model Member {

идентификатор Int @id @default(автоинкремент())

Идентификатор пользователя Int

Идентификатор сервера Int

  

пользователь Пользователь @relation(поля: [userId], ссылки: [id])

сервер Сервер @relation(поля: [serverId], ссылки: [id], при удалении: каскадное удаление)

}



// --- КАНАЛ ---

модельный канал {

идентификатор Int @id @default(автоинкремент())

строка имени

type String @default("text") // "text" или "voice"

  

serverId Int

server Server @relation(fields: [serverId], references: [id], onDelete: Cascade)

  

messages Message[] // Сообщения в этом канале

}



// --- СООБЩЕНИЕ ---

model Message {

id Int @id @default(autoincrement())

content String? // Текст сообщения (может быть пустым, если это картинка)

imageUrl String? // Ссылка на картинку (base64)

type String @default("text") // "text", "image", "call"

  

isEdited Boolean @default(false) // Флаг: было ли сообщение изменено

  

author String // Имя автора (для удобства)

userId Int

user User @relation(fields: [userId], references: [id])



// Если сообщение в канале сервера

channelId Int?

channel Channel? @relation(fields: [channelId], references: [id], onDelete: Cascade)

  

// Если сообщение в ЛС (название комнаты, например "dm_1_2")

dmRoom String?



createdAt DateTime @default(now())

  

reactions Reaction[] // Реакции на это сообщение

}



// --- РЕАКЦИЯ (НОВАЯ) ---

модельная реакция {

идентификатор Int @id @default(автоинкремент())

Строка с эмодзи // Сам смайлик (например, «👍»)

  

Идентификатор пользователя Int

пользователь Пользователь @relation(поля: [userId], ссылки: [id])

  

MessageId Int

сообщение Сообщение @relation(поля: [messageId], ссылки: [id], при удалении: каскадное удаление)

}



// --- УВЕДОМЛЕНИЕ ---

уведомление о модели {

идентификатор Int @id @default(автоинкремент())

тип данных: строка // «FRIEND_REQUEST», «SERVER_INVITE»

статус Строка @default("В ОЖИДАНИИ") // "ПРИНЯТО", "ОТКЛОНЕНО"

  

Отправительid Int

отправитель Пользователь @relation("Отправитель", поля: [senderId], ссылки: [id])

  

Идентификатор получателя Int

получатель Пользователь @relation("Получатель", поля: [получательId], ссылки: [id])

  

// Опционально: ссылка на сервер (для приглашений)

serverId Int?

server Server? @relation(fields: [serverId], references: [id], onDelete: Cascade)

  

createdAt DateTime @default(now())

}